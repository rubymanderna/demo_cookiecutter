name: docs_mf_2024
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - numpy 
  
  - pydata-sphinx-theme
  - sphinx-design
  - sphinx-copybutton




    # Pip-only installs
  - pip:
    - -e ../

